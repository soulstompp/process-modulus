# DID ANYBODY LOOK FOR DOUBLE COUNTING ACROSS THIS FUSION'S PARTS? One row per composed layer
# that was asked, carrying what it answered.
#
# ⭐⭐⭐ THE ANSWER DECIDES WHICH ARITHMETIC IS OWED, which is what raises this above bookkeeping.
#    `none` or `notApplicable`: the composed figure must equal the sum of its converted parts
#    EXACTLY. `unmeasured`: no equality is owed at all, and a checker reporting one is reporting
#    about nothing. eliminations/unsearched.sqlc is that third state made a relation.
#
# ⛔ A LIST CHECKED CLEAN AND A LIST NOBODY LOOKED AT ARE THE SAME BYTES AND THEY OWE OPPOSITE
#   ARITHMETIC. The eliminations table cannot tell them apart; this can.
-- asrt:Fusion/asrt:Eliminations/asrt:Absent, one row per composed layer asked.
SELECT es.composition, es.composed_layer, es.absent AS answer, es.note
FROM pm.elimination_search es
